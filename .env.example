# Optional: override Arc's public Testnet RPC endpoint.
ARC_RPC_URL=https://rpc.testnet.arc.network

# Set this to the final deployment URL for absolute social metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
